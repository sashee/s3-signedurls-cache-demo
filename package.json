{
  "name": "cacheable_signed_urls",
  "version": "1.0.0",
  "description": "aws s3api create-bucket --bucket signedurls-test --create-bucket-configuration LocationConstraint=eu-west-1 --region eu-west-1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.447.0",
    "express": "^4.16.4",
    "pug": "^2.0.3",
    "timekeeper": "^2.2.0"
  }
}
